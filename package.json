{
  "name": "github-virtual-collaborators",
  "version": "1.0.0",
  "description": "GitHub Action for managing virtual collaborators via tags and notifications",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && ncc build src/index.ts -o dist",
    "test": "node --experimental-vm-modules ./node_modules/jest/bin/jest.js",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.5",
    "@vercel/ncc": "^0.38.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  }
}
